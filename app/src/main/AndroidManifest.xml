<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.campusexpensemanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/campusexpense__2_"
        android:label="@string/app_name"
        android:roundIcon="@drawable/campusexpense__2_"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusExpenseManager.NoActionBar"
        tools:targetApi="34">

        <!-- Splash Activity as launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CampusExpenseManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity with AppCompat theme -->
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.CampusExpenseManager.NoActionBar"
            android:windowSoftInputMode="adjustPan"
            android:exported="true" />

        <!-- Other activities -->
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="true" />

        <activity android:name=".AccountActivity"></activity>


    </application>
</manifest>
